# On macOS npm lives in the user-writable Homebrew prefix; running it as
# root would leave root-owned files there and break brew
- name: Install Gemini CLI globally
  become: "{{ ansible_facts.system != 'Darwin' }}"
  npm:
    name: "@google/gemini-cli"
    global: true
    state: present
  tags:
    - gemini

