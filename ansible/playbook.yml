- name: Bootstrap local machine
  hosts: localhost
  connection: local

  roles:
    - core
    - fonts
    - dotfiles
    - bash
    - zsh
    - nodejs
    - go
    - gemini_cli
    - claude_code
    - claude_status_line
    - terminal
    - neovim
    - keyboard_layout
