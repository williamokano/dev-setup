- name: Bootstrap local machine
  hosts: localhost
  connection: local

  roles:
    - core
    - fonts
    - dotfiles
    - bash
    - zsh
    - nodejs
    - go
    - gemini_cli
    - terminal
    - neovim
    - keyboard_layout
